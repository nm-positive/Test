name: Python package

on: [push]

jobs:
  build:
    runs-on: windows--latest
    strategy:
      matrix:
        python-version: ["pypy3.9", "pypy3.10", "3.9", "3.10", "3.11", "3.12"]
    steps:
      - name: Sample
        run: |
          echo "Hello World"
